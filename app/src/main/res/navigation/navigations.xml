<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigations"
    app:startDestination="@id/mainFragment">

    <fragment
        android:id="@+id/mainFragment"
        android:name="id.taufiq.MainFragment"
        android:label="fragment_main"
        tools:layout="@layout/fragment_main">
        <action
            android:id="@+id/action_mainFragment_to_bottomDialog"
            app:destination="@id/bottomDialog" />
    </fragment>
    <dialog
        android:id="@+id/bottomDialog"
        android:name="id.taufiq.helper.BottomDialog"
        android:label="BottomDialog" />
</navigation>